//! Application jobs (loaded by `queue-worker`).

#![forbid(unsafe_code)]

// Export modules so job files compile and can submit inventory entries.
// (Auto-registration is done via `#[job]` proc-macro.)
// purwa:jobs-mods
// purwa:end-jobs-mods

