//! {{ name }} job.

#![forbid(unsafe_code)]

use purwa::job;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
#[job(type = "{{ type_string }}")]
pub struct {{ name }} {
    // TODO: add fields
}

impl {{ name }} {
    pub async fn perform(self, ctx: purwa_queue::JobContext) -> Result<(), String> {
        let _ctx = ctx;
        // TODO: perform work
        Ok(())
    }
}

